syntax = "proto3";

import "image_1.proto";

message ImageOcrResult {
    Image image = 1;
    string text = 2;
}

message ImageOcrResults {
    repeated ImageOcrResult imageOcrResult = 1;
    float sentAt = 6;
}

service OcrBrick {
    rpc process (Images) returns (ImageOcrResults) {
    }
}
